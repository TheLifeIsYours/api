import express from 'express';
const app = express();

import * as fs from 'fs';
import * as path from 'path';

//Read routes from root ./routes folder and crawl through every subfolder and apply to express with app.use(folder_name, route)




export default app;